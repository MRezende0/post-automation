# Plataforma — copie pra .env.local (dev) ou configure no Vercel (prod).

# Supabase (server-side, service_role — NUNCA expor no client)
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_KEY=eyJ...

# Gate de acesso (single-user). Senha de login + segredo pra assinar o cookie.
PLATFORM_PASSWORD=troque-isto
AUTH_SECRET=gere-um-hex-aleatorio-de-32-bytes

# Protege o endpoint do cron (Vercel manda este header automaticamente nos crons
# se configurado; em dev/local pode chamar com ?key= ou header x-cron-key).
CRON_SECRET=outro-hex-aleatorio

# Telegram — só notificações (sem botões)
TELEGRAM_BOT_TOKEN=123:abc
TELEGRAM_CHAT_ID=123456

# Instagram Graph API
IG_ACCESS_TOKEN=
IG_BUSINESS_ACCOUNT_ID=

# LinkedIn API
LINKEDIN_ACCESS_TOKEN=
LINKEDIN_AUTHOR_URN=urn:li:person:xxxx

# Fuso pra resolver os slots editoriais (offset fixo em horas; BRT = -3)
PUBLISH_TZ_OFFSET=-3
